# 이 저장소에서 에이전트가 지킬 것

이 저장소는 **클로드 코드(Claude Code)** 에 맞춰 설정되어 있습니다.
지켜야 할 것은 전부 **[`CLAUDE.md`](CLAUDE.md)** 에 있습니다. 그 파일을 읽고 그대로 따르세요.

한 줄로 줄이면 이렇습니다.

- 사용자는 **개발을 모릅니다.** 터미널 명령을 보여주지 말고, 필요한 실행은 당신이 합니다.
- 사용자가 눌러야 할 것이 있으면 **웹 주소 한 줄**만 줍니다.
- **키는 묻지도, 대신 넣지도 마세요.** `.env` 는 읽지도 고치지도 않습니다.
- 고치는 파일은 **`strategy.py` 하나**입니다. 고친 뒤에는 `python check.py` 와
  `python -m unittest discover -s tests` 를 둘 다 통과시킵니다.
- 실제 돈으로 넘어가는 절차(「실전투자」)와 예약 만드는 법은 `CLAUDE.md` 에 있습니다.

설정 파일도 같이 봅니다.

| 파일 | 하는 일 |
|---|---|
| `CLAUDE.md` | 에이전트 지침 (이 파일의 본문) |
| `.claude/settings.json` | 자주 쓰는 명령은 허용, `.env` 접근은 차단 |
| `.claude/commands/` | `/board` `/setup` `/check` `/scan` `/live` |
| `schedule.txt` | 예약이 회차마다 읽는 글 |
