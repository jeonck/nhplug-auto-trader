# 이 저장소에서 클로드 코드가 지킬 것

이 프로젝트를 쓰는 사람은 **개발을 모릅니다.** 터미널을 열 줄 모르고, 파일 편집기도
못 엽니다. 그것을 전제로 움직이세요.

- **명령줄을 사용자에게 보여주지 마세요.** 실행이 필요하면 당신이 Bash 로 직접 실행합니다.
- 사용자가 무언가 눌러야 한다면 **웹 주소 한 줄**만 주세요. 그것이 유일하게 허용된
  요청입니다.
- 서버에서 웹 화면을 띄웠으면 **포트 전달(웹 미리보기)** 로 이어 주세요. 로그에
  `FORWARD_PORT=숫자` 가 찍혀 있습니다. 사용자의 컴퓨터에서 돌고 있다면 그냥
  `http://127.0.0.1:그포트` 를 주면 됩니다.
- 로그에 `ssh` 나 방화벽 이야기가 나오면 그건 **당신에게** 하는 말입니다. 사용자에게
  옮기지 마세요. 당신이 못 하면 "제 쪽에서 안 됩니다"라고만 말하세요.
- **키나 비밀번호는 묻지도, 대신 넣지도 마세요.** 사용자가 화면에서 직접 넣습니다.
- `.env` 는 절대 읽지도 고치지도 마세요. 계좌 전환은 설정 화면에서만 합니다.
  (`.claude/settings.json` 이 `.env` 접근을 막아 둡니다. 막혔다고 우회하지 마세요.)
- 사용자가 고쳐 달라고 한 파일은 **`strategy.py` 하나**입니다. 시키지 않은 리팩터링,
  테스트 추가, 문서 정리를 스스로 하지 마세요.
- **한국어로 답하세요.** 숫자는 통화 단위(원 / $)를 붙여서 말합니다.

## 슬래시 커맨드

자주 쓰는 절차는 `.claude/commands/` 에 있습니다. 사용자가 아래 한국어를 치면
같은 일을 하면 됩니다.

| 사용자가 치는 말 | 커맨드 | 하는 일 |
|---|---|---|
| **현황** / **얼마나 벌었어** | `/board` | `board.py` 를 띄우고(이미 떠 있으면 그대로) 주소만 주기 |
| **설정** | `/setup` | `python setup.py` 를 띄우고 주소만 주기 |
| **모의투자** | `/setup` | 설정 화면을 띄운 뒤, 1단계에서 모의투자로 되돌리라고 안내 |
| **실전투자** | `/live` | 아래 「실전투자」 절차 |
| **검사** / **점검** | `/check` | 테스트와 전략 검사를 돌리고 결과만 말하기 |
| **한 번 돌려봐** | `/scan` | `schedule.txt` 대로 한 회차만 실행 |

## 이 저장소에서 쓰는 명령

당신이 직접 실행합니다. 사용자에게 시키지 않습니다.

```bash
python trade.py --scan            # 상황을 살피고 판단이 필요한 종목을 JSON으로 (손절·익절만 주문)
python trade.py --do <파일>       # 판단을 검증하고 주문 (한글이 깨지므로 반드시 파일로)
python trade.py --preview         # 상황만 보여 줌. 주문하지 않음
python trade.py --account         # 계좌·한도 확인. 주문하지 않음
python check.py                   # 전략 검사
python -m unittest discover -s tests   # 프로젝트 자체 검사
```

`.venv` 가 있으면 `.venv/bin/python` 을 씁니다. 웹 화면(`setup.py` · `board.py`)은
`nohup ... &` 로 띄워 백그라운드에 두고, 로그에서 포트를 읽어 주소만 건네줍니다.

## 예약(scheduled task)

터미널을 켜 두지 않습니다. **1시간마다** 예약이 부릅니다. 사용자가 "예약 만들어 줘"
라고 하면 당신이 만듭니다.

1. 당신에게 예약(scheduled task / cron) 기능이 있으면 그것으로 **1시간 간격** 작업을
   만듭니다. 실행 위치는 이 폴더입니다.
2. 기능이 없거나 원격 서버라면 그 서버의 crontab 에 넣습니다.

   ```bash
   0 * * * * cd ~/nhplug-auto-trader && claude -p "$(cat schedule.txt)" >> trade.log 2>&1
   ```

   `.claude/settings.json` 의 허용 목록 덕분에 `-p`(헤드리스)로 돌아도 매번 권한을
   묻지 않습니다. 그래도 막히면 `--allowedTools` 로 같은 목록을 넘깁니다.

**예약 칸에는 내용을 붙여넣지 않습니다.** 이 두 줄만 넣습니다.

```
이 폴더의 schedule.txt 파일을 읽고, 거기 적힌 대로 그대로 해.

그 파일에 없는 일은 하지 마. 파일을 못 찾으면 아무것도 하지 말고
"schedule.txt 를 못 찾았다"고만 알려 줘.
```

그래야 시키는 내용이 바뀌어도 예약을 다시 고칠 필요가 없습니다.

만든 뒤에는 등록됐는지 확인하고 첫 회차 결과를 보여 준 다음, "멈추고 싶으면 예약을
일시 중지하면 된다"고 알려 줍니다.

## 「실전투자」 절차

사용자가 **실전투자**라고 하면, 실제 돈으로 넘어가겠다는 뜻입니다.

1. `git pull` 로 최신 코드를 받습니다. `strategy.py` 가 충돌하면 사용자의 종목·금액·
   `INSTRUCTIONS` 는 그대로 두고 주석만 새것으로 맞춥니다.
2. `python -m unittest discover -s tests` 와 `python check.py` 를 돌립니다.
   **하나라도 실패하면 거기서 멈추고 사용자에게 알립니다.** 실제 돈이 걸린 일입니다.
3. `python trade.py --account` 로 지금 한도를 확인해서, 아래를 **숫자로** 보여 줍니다.

   > 지금 설정이라면 한 종목에 얼마, 최대 몇 종목, 최대 얼마까지 들어갑니다.
   > 손절은 몇 %, 익절은 몇 %입니다.

   그리고 **눈에 걸리는 것이 있으면 한 줄씩 짚어 줍니다.** 고치라고 시키지 말고,
   무슨 뜻인지만 알려 주세요. 판단은 사용자가 합니다.

   - 익절이 손절보다 작으면 → "이기는 거래 하나가 지는 거래 하나보다 작습니다.
     그만큼 자주 이겨야 본전입니다."
   - 프리마켓·애프터마켓이 켜져 있으면 → "사려는 사람이 적은 시간대라 원하는 값에
     안 걸리거나 불리하게 걸립니다. 지표는 아직 어제 것입니다."
   - 최대 한도가 사용자가 말한 예산보다 크면 → 그 차이를 그대로 말해 줍니다.

4. 그다음 **반드시 이것을 묻습니다.** (`AskUserQuestion` 으로 두 개를 고르게 하면
   가장 깔끔합니다.)

   > 실제 돈으로 하기 전에, 전략을 어떻게 할까요?
   >   ① 지금 전략 그대로 갑니다
   >   ② 실제 돈 기준으로 상담을 다시 합니다 (금액·손절선·시간대를 다시 정함)

   ②를 고르면 **설정 화면을 띄우기 전에** 상담부터 합니다. 상담 방식은 `setup.py`
   안의 프롬프트와 같되, 시작할 때 이 세 가지를 분명히 하세요.

   - **모의투자 때 쓰던 금액은 잊으라고 하세요.** 가짜 돈 5억에 맞춰 정한 숫자가
     그대로 남아 있으면 실제 계좌에서 너무 큽니다.
   - **"잃어도 생활에 지장 없는 금액"이 얼마인지** 먼저 묻고, 거기서 한 종목
     금액과 최대 종목 수를 거꾸로 계산하세요.
   - 프리마켓·애프터마켓을 계속할지 다시 확인하세요(`US_SESSIONS`).

   상담이 끝나면 `strategy.py` 를 고치고, `python check.py` 와
   `python -m unittest discover -s tests` 를 **둘 다 통과시킨 뒤** 바뀐 한도를
   다시 숫자로 보여 주고 "이대로 실제 돈으로 하시겠어요?"라고 물으세요.

5. 사용자가 좋다고 하면 그때 설정 화면을 띄웁니다.

   ```
   nohup .venv/bin/python setup.py --live > setup.log 2>&1 &
   ```

   `setup.log` 의 `FORWARD_PORT=숫자` 로 주소를 만들어 **주소만** 줍니다.
   `--live` 로 열면 화면이 순서(① Telegram ② 실제 계좌)를 스스로 안내합니다.
6. 사용자에게 이렇게만 말합니다.

   > 이 주소에서 ① Telegram을 먼저 연결하고 ② [실제 계좌]를 고른 뒤 저장하세요.
   > 키는 다시 넣지 않아도 됩니다. 다 되면 "다 했다"고 말해 주세요.

7. 사용자가 "다 했다"고 하면 `python trade.py --account` 로 `실제 계좌` 로 바뀌었는지
   확인하고, 바뀌었으면 알려 줍니다. 안 바뀌었으면 무엇이 남았는지 짚어 줍니다.
8. 마지막으로 이것을 알려 줍니다.

   > 이제부터 주문마다 Telegram으로 확인이 갑니다. **3분 안에 승인하지 않으면
   > 취소됩니다.** 손절·익절은 승인 없이 규칙이 바로 팝니다.
   > 되돌리려면 "모의투자"라고 말씀하세요.

**금액을 당신이 임의로 바꾸지 마세요.** 사용자가 줄여 달라고 하면 그때만 `strategy.py`
의 `BUY_AMOUNT` · `US_BUY_AMOUNT` · `MAX_HOLDINGS` 를 고치고, 고친 뒤 `python check.py`
를 통과시키세요.

## 전략을 고칠 때

`strategy.py` 맨 위 주석에 지켜야 할 약속이 전부 적혀 있습니다. 그대로 따르세요.
설정 화면 2단계의 상담 프롬프트(`setup.py` 의 `AGENT_PROMPT`)와 같은 내용입니다.

- 상담은 **질문 하나씩**, 6~8번 주고받고 끝냅니다. 목록으로 늘어놓으면 설문지가 됩니다.
- 고치기 전에 정리해서 보여 주고 "이대로 할까요?" 를 먼저 묻습니다.
- 고친 뒤에는 `python check.py` 와 `python -m unittest discover -s tests` 를 둘 다
  통과시킵니다. **통과 못 한 전략은 저장하지 않습니다.**
- `strategy.py` 안에서는 네트워크·파일·프로세스 모듈을 쓰지 않습니다(`check.py` 가 막습니다).

## 이 프로젝트의 구조

| 파일 | 하는 일 |
|---|---|
| `setup.py` | 설정 웹페이지 (키 입력·연결 확인·상담 안내) |
| `board.py` | 현황 웹페이지. **보기만 합니다** |
| `broker.py` | NH PLUG 공식 API 호출 (국내 + 미국) |
| **`strategy.py`** | **투자 원칙과 넘지 말 선. 이 파일만 바뀝니다** |
| `trade.py` | `--scan` 으로 살피고 `--do` 로 주문 |
| `check.py` | 전략 검사 |
| `telegram.py` | 실거래 주문을 Telegram에서 한 번 승인 |
| `schedule.txt` | 예약에 넣을 글. 화면·문서·cron이 이 파일 하나를 씁니다 |

**판단은 클로드 코드가, 주문은 코드가 냅니다.** 예산·지정가·승인·재시도 금지는
`trade.py --do` 가 판단과 무관하게 다시 검증합니다. `broker.py` 를 직접 부르거나
다른 방법으로 주문하지 마세요.
